# 2 OR v5.1 — รายงานตรวจสอบและรายการปรับปรุง

วันที่ตรวจ: 24 กันยายน 2026

## ผลตรวจ

- JavaScript syntax check: **ผ่าน**
- เปิดไฟล์จริงด้วย Chromium: **ผ่าน**
- Self-test ของระบบ: **45/45 ผ่าน**
- ข้อมูลเดิม: ใช้โครงสร้างเดิมและมี migration แบบไม่ลบข้อมูล
- ไฟล์ใช้งาน: `2OR_v5__1790189391978.html`

## สิ่งที่ตรวจพบในไฟล์เดิม

1. เมนูมีจำนวนมากและใช้แถบเลื่อนแนวนอน จึงดูเหมือนเมนูหายเมื่อเปิดบนจอแคบ
2. การสลับผู้ใช้อยู่ในหน้าตั้งค่า ทำให้หาไม่เจอจากหน้าอื่น
3. ระบบเดิมมี Integrity Check, Audit Log และ self-test อยู่แล้ว แต่ยังไม่มีรายงาน Spot/Deep แบบส่งออกได้
4. ระบบเดิมเก็บข้อมูลใน browser เป็นหลัก และมีส่วน Cloud Sync แบบ Firebase ที่ขึ้นกับการตั้งค่า Firestore

## สิ่งที่เพิ่มใน v5.1

### 1. สลับบัญชีและเมนูตั้งค่าที่เข้าถึงง่าย

- เพิ่มตัวเลือกสลับบัญชีที่แถบด้านบนทุกหน้า
- ปุ่มชื่อผู้ใช้และปุ่มตั้งค่าด้านบนเปิดหน้าตั้งค่าได้ทันที
- ยังคงตรวจรหัสผ่านก่อนสลับบัญชี
- บันทึกเหตุการณ์การสลับบัญชีลง Audit Log

### 2. สมุดสูตรแบบแบ่งช่อง

- ตารางสูตร 6 คอลัมน์ A–F และเริ่มต้น 12 แถว
- ใส่ตัวเลข ข้อความ หรือสูตรในแต่ละช่องได้
- รองรับ:
  - `+ - * /`
  - วงเล็บ
  - การอ้างอิงเซลล์ เช่น `=A2*B2`
  - ช่วงเซลล์ เช่น `=SUM(A2:A5)`
  - `SUM`, `AVERAGE`, `MIN`, `MAX`, `COUNT`, `ABS`, `ROUND`, `IF`
- ดาวน์โหลดตารางสูตรเป็น CSV ได้
- มีการตรวจสูตรวนกลับและการหารด้วยศูนย์

### 3. ซัพพลายเออร์และเปรียบเทียบต้นทุน

- เพิ่มรายชื่อซัพพลายเออร์
- เพิ่มราคาเสนอแยกตามสินค้า
- บันทึกต้นทุน/หน่วย ค่าขนส่ง/หน่วย MOQ และระยะเวลาจัดส่ง
- เปรียบเทียบต้นทุนรวมต่อหน่วยและทำเครื่องหมายราคาต่ำสุด

### 4. Performance Scorecard, Bin Location และ Reorder Point

- ให้คะแนนซัพพลายเออร์ 0–5 ด้านคุณภาพ ส่งตรงเวลา ต้นทุน และบริการ
- ระบุ Bin Location ของสินค้า เช่น `A-01-03`
- ระบุ Reorder Point แยกรายสินค้า
- แจ้งรายการที่ถึงจุดสั่งซื้อจากยอดคงเหลือจริง

### 5. Spot Check + Deep Audit Log

- Spot Check ตรวจความสอดคล้องของสต็อก ล็อต ยอดเงิน และรายการขาย
- Deep Audit เพิ่มการตรวจ Audit Log ราคาเสนอ และสูตร
- แสดงจำนวนจุดที่ตรวจและรายการผิดปกติ
- ดาวน์โหลดรายงาน JSON ได้

## ข้อควรระวังก่อนเผยแพร่

- ถ้าเปิดใช้แบบไม่เชื่อม Cloud ข้อมูลจะอยู่ใน browser เครื่องนั้นเท่านั้น
- ควรกด **ตั้งค่า → สำรองข้อมูล** และเก็บไฟล์ JSON ไว้เป็นระยะ
- ถ้าเผยแพร่เป็นเว็บไซต์สาธารณะ อย่าใส่ข้อมูลลูกค้า เลขบัญชี หรือข้อมูลลับจริงในไฟล์นี้จนกว่าจะตั้ง Firebase Authentication และ Firestore Security Rules อย่างปลอดภัย
- Firebase config ที่อยู่ในหน้าเว็บไม่ใช่ตัวควบคุมสิทธิ์ข้อมูล สิทธิ์จริงต้องกำหนดที่ Firestore Rules

## วิธีใช้งานออนไลน์แบบฟรี

### วิธีที่ง่ายที่สุด: Netlify Drop

1. เปลี่ยนชื่อสำเนาไฟล์เป็น `index.html`
2. สร้างโฟลเดอร์ใหม่ เช่น `2or-online`
3. ใส่ `index.html` ไว้ในโฟลเดอร์นั้น
4. เข้า `https://app.netlify.com/drop`
5. สมัครหรือล็อกอินบัญชีฟรี
6. ลากโฟลเดอร์ `2or-online` ไปวางในหน้าเว็บ
7. รออัปโหลดเสร็จ แล้วเปิด URL ที่ Netlify แสดง
8. บันทึก URL ไว้เปิดจากโทรศัพท์หรือคอมพิวเตอร์เครื่องใดก็ได้
9. เมื่อต้องการอัปเดต ให้แก้ไฟล์แล้วลากโฟลเดอร์เดิมขึ้นใหม่

วิธีนี้ทำให้หน้าเว็บเข้าถึงได้จากทุกที่ แต่ `localStorage` ของแต่ละ browser ยังแยกกันอยู่

### วิธีฟรีและควบคุมไฟล์ได้: GitHub Pages

1. สร้างบัญชี GitHub ฟรี
2. สร้าง repository ใหม่เป็น Public
3. อัปโหลดไฟล์โดยใช้ชื่อ `index.html`
4. เปิดแท็บ **Settings → Pages**
5. ใน **Build and deployment** เลือก **Deploy from a branch**
6. เลือก branch `main` และโฟลเดอร์ `/root`
7. กด Save
8. รอ GitHub สร้างเว็บไซต์ แล้วเปิด URL รูปแบบ:
   `https://ชื่อผู้ใช้.github.io/ชื่อ-repository/`

ถ้าข้อมูลเป็นข้อมูลธุรกิจจริง ให้ใช้ GitHub Pages หรือ Netlify เป็นที่วางหน้าเว็บเท่านั้น และอย่าเปิด Firestore เป็น public write โดยไม่มีระบบยืนยันตัวตน

## v6.0 — แก้ข้อมูลไม่ตามหน้าและเพิ่มทะเบียนพนักงาน

- แก้ลำดับการซิงค์: เครื่องใหม่จะอ่านข้อมูลจาก Cloud ก่อน ไม่ส่งข้อมูลเริ่มต้นในเครื่องไปทับข้อมูลเดิม
- เปลี่ยนการซิงค์จากเอกสารบริษัทเดียวเป็น `company/{companyId}/records` โดยแยกเอกสารตามชนิดข้อมูลและรหัสรายการ
- บิล การเคลื่อนไหวสต็อก รายการเงิน ค่าคอม และ Audit แต่ละรายการมีเอกสารของตัวเอง จึงลดความเสี่ยงจากการเขียนพร้อมกันหลายเครื่อง
- ยังคงใช้ Local Cache เพื่อให้เปิดหน้าและทำงานได้แม้เน็ตช้า พร้อมซิงค์เบื้องหลังเมื่อ Cloud พร้อม
- เพิ่มทะเบียนพนักงานแยกจากบัญชีผู้ใช้
- รองรับค่าจ้างพื้นฐานรายคนแบบรายเดือน รายวัน หรือเหมาจ่าย
- หน้าค่าคอมแสดงค่าจ้างพื้นฐาน คอมค้างจ่าย และยอดประมาณการรวมของเดือน
- ชื่อพนักงานเดิมที่พบจากรายการขายหรือค่าคอมจะถูกเพิ่มเข้าทะเบียนอัตโนมัติแบบไม่ลบข้อมูลเดิม

### ขอบเขตด้านข้อมูลจำนวนมาก

การแยกเอกสารช่วยแก้ปัญหาการทับกันและข้อจำกัดของเอกสารบริษัทเดียว แต่หน้าเว็บแบบไฟล์เดียวจะยังโหลดข้อมูลที่ใช้ทำรายงานจาก Browser ในครั้งแรก หากมีบิลหลักแสนจริง ควรแยก API/Cloud Functions สำหรับรายงานแบบแบ่งหน้าและสรุปยอด เพื่อไม่ต้องโหลดประวัติทั้งหมดเข้าหน่วยความจำของโทรศัพท์